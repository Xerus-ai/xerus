'use client'

// This is now a placeholder since login is handled by ClientLayout overlay
export default function LoginPage() {
  // The actual login UI is rendered by ClientLayout when pathname === '/login'
  // This component is no longer used directly
  return null
} 