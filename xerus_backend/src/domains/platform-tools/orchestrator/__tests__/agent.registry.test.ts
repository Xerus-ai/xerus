// Agent Registry Tests (DEPRECATED)
// Original tests for the pre-CLI-native Agent SDK configuration builder.
// The module under test (agent.registry.ts) was dead code after the CLI-native pivot.
// Deleted in cleanup: 2026-04-03.
// See: mcp-config.service.ts for the replacement Pipedream MCP integration.

describe('agent.registry (deprecated)', () => {
    it('module replaced by mcp-config.service.ts', () => {
        expect(true).toBe(true);
    });
});
